{
  "name": "ghost-shell-contracts",
  "version": "1.0.0",
  "description": "Ghost Shell Smart Contracts for DeFi Security",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network hashkeyTestnet",
    "authorize": "hardhat run scripts/authorize-backend.ts --network hashkeyTestnet",
    "verify": "hardhat verify"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
